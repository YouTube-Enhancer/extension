// The marks the feature leaves on the page. Shared with the specs.
/** Set on a video container while its title is masked. */
export const BLOCKED_ATTRIBUTE = "data-yte-keyword-blocked";
/** Set on the body while the pointer rests on a masked container. */
export const HOVER_BLOCKED_CLASS = "yte-hover-blocked";
